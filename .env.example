# URL base da API (inclua o prefixo /api).
# Em DESENVOLVIMENTO, deixe vazio: o proxy do Vite encaminha /api para o
# backend em http://localhost:3334 (ver vite.config.js).
# Em PRODUÇÃO, aponte para a API publicada, por exemplo:
#   VITE_API_URL=https://api.seu-dominio.com/api
VITE_API_URL=
